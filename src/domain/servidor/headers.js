export const GET = {
    headers: new Headers({
        "Accept":"application/json"
    })
}

export const POST = {
    method:"POST",
    headers: new Headers({
        "Accept":"application/json"
    })
}